

char* select(char* type, char* prop, char* value); //seleciona através de critério de pesquisa e valor {prop -> value}
int save(char* stream, char* type); //guarda dados
int mod(char type, char* old, char* mod); //modificar dados